# Duplicate File Finder

A command line tool that scans a directory and detects duplicate files using hashing.

## Features

- Detect duplicate files
- Uses file hashing for accuracy
- Works recursively through directories

## Usage

python main.py /path/to/folder
